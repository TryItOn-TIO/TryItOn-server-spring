spring.profiles.active=local

spring.application.name=core

# --- ???? ?? ?? ?? ?? ---

# 1. AWS Secrets Manager ?? ?? tio/db/credentials ? tio/oauth/google ??? ????? ?????.
spring.config.import=aws-secretsmanager:tio/db/credentials,aws-secretsmanager:tio/oauth/google

# 2. DataSource ?? (MySQL/RDS ?)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# URL, username, password? Secrets Manager? ???? ?????.

# 3. JPA/Hibernate ?? (MySQL/RDS ?)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 4. Actuator ??
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# oauth
spring.security.oauth2.client.registration.google.client-id=${OAUTH_ID}
spring.security.oauth2.client.registration.google.client-secret= ${OAUTH_PASSWORD}
spring.security.oauth2.client.registration.google.scope=email,profile


# AWS health ?????? ????, ?????? ??? ?? ?? ??
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never